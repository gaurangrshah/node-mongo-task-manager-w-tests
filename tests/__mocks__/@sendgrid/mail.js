module.exports = {
  setApiKey() {
    // this can be kept blank beacuse we are not returning anything from the actual function, so we dont need to return anything here, just need to instantiate it so that the test does not fail.
  },
  send() {
    // this can be kept blank beacuse we are not returning anything from the actual function, so we dont need to return anything here, just need to instantiate it so that the test does not fail.
  }
}
